# UTF-8
#
# For more info about fixed file info structure:
# http://msdn.microsoft.com/en-us/library/ms646997(v=vs.85).aspx
#
# PyInstaller will try to use this file to create the version info resource
# in the executable file.
VSVersionInfo(
  ffi=FixedFileInfo(
    # filevers and prodvers should be always a tuple with four integers
    filevers=(1, 22, 0, 0),
    prodvers=(1, 22, 0, 0),
    # Set the appropriate bitmask for the file flags.
    # Valid values for fileflags are:
    # VS_FF_DEBUG, VS_FF_PRERELEASE, VS_FF_PATCHED, VS_FF_PRIVATEBUILD,
    # VS_FF_INFOINFERRED, VS_FF_SPECIALBUILD
    mask=0x3f,
    flags=0x0,
    # FILEOS is unknown: VOS__WINDOWS32 (Windows 32-bit)
    OS=0x00040004,
    # FILETYPE is unknown: VFT_APP (application)
    fileType=0x00000001,
    # FILESUBTYPE is 0 (reserved)
    subtype=0x00000000,
    date=(0, 0),
    ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        u'040904B0',
        [StringStruct(u'CompanyName', u'GAC'),
        StringStruct(u'FileDescription', u'MeetTimer - Meeting Timer'),
        StringStruct(u'FileVersion', u'1.22.0.0'),
        StringStruct(u'InternalName', u'MeetTimer'),
        StringStruct(u'LegalCopyright', u'Copyright 2026'),
        StringStruct(u'OriginalFilename', u'MeetTimer.exe'),
        StringStruct(u'ProductName', u'MeetTimer'),
        StringStruct(u'ProductVersion', u'1.22.0.0')])
      ]),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)
