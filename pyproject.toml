[project]
name = "hui-bang-shou"
version = "1.20.0"
description = "会帮手 - Windows Meeting Topic Timer"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.1.5",
    "pyside6==6.8.3",
]

[project.scripts]
gac-timer = "main:main"