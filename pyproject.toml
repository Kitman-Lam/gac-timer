[project]
name = "gac-timer"
version = "0.1.0"
description = "GAC Timer - Windows Meeting Topic Timer"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.1.5",
    "pyside6==6.8.3",
]

[project.scripts]
gac-timer = "main:main"
